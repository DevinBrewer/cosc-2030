#include "TrackingTree.h"
#include <iostream>
using namespace std;

int main() {

  // Create a single node to test its functionality
  Node snode = Node("parentID");

  snode.printData();

  return 0;
}
